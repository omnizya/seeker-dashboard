create table if not exists magick.square_calculations ( id bigint generated always as identity primary key, element magick.element not null, input_value integer not null, output_square integer[] not null, magic_constant integer not null, calculated_by uuid not null references identity.profiles (id) default auth.uid (), tenant_id uuid references core.tenants (id), notes text, created_at timestamptz default now() );
