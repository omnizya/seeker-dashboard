create table if not exists spiritual.abjad_calculations ( id bigint generated always as identity primary key, holy_name_id bigint not null references spiritual.holy_names (id) on delete cascade, calculated_value integer not null, calculated_by uuid not null references identity.profiles (id), tenant_id uuid references core.tenants (id), notes text, created_at timestamptz default now() );
